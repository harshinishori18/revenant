# Optional. Without it Revenant runs fully and the optimiser decides every
# payment; with it, ambiguous cases (top two actions within 8% expected value)
# route to the LLM tie-breaker.
ANTHROPIC_API_KEY=
